public class Code14_operator_binaryNumberSys {
    public static void main(String[] args) {
        //2+3 here 2 and 3 are operand and + is operator

        /*
        *binary operator works on two operands
        (i) +,-,*,/,%
        *unary operator work on single operands
        (ii) ++,--
        ++i will change first value then give value
        i++ will give first value then change its value
        similarly with --i and i--
        *Relational operator
        ==,!=,>,<,>=,<=
        * Logical operator
        && ,||,!
        * Bitwise operator
        &(binary AND),|(Binary OR),^(XOR),~(one's complement),<<(left shift),>>(right shift)
        * AND - if both 0,0 and 1,0 then will give always 0 and if same 1,1 then result in 1
        * OR - if 0,0 then give 0 otherwise always 1
        * XOR -if 0,1 then 1 otherwise whatever 0,0 or 1,1 will give 0 always
        * left shift - will push end digit one step back
        * right shift - will push right most digit one step ahead
         */
    }
}
